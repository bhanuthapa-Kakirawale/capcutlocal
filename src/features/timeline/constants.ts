export const TRACK_HEIGHT_PX = 56;
export const RULER_HEIGHT_PX = 28;
/** On-screen distance within which a dragged edge snaps to a target (docs/TIMELINE.md §6). */
export const SNAP_THRESHOLD_PX = 8;
